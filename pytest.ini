[pytest]
# Python 3.14 + esta versão do pytest têm um bug na reescrita de asserts (`ast.Str`
# foi removido). Desligar a reescrita evita o crash sem perder nenhuma checagem.
addopts = --assert=plain -p no:cacheprovider
testpaths = tests
